{
  "name": "usdc-batch-tx-poc",
  "version": "1.0.0",
  "description": "PoC for batching USDC approve and transfer transactions using viem on Base Sepolia",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "ethers": "^6.15.0",
    "viem": "^2.38.5"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "typescript": "^5.5.0",
    "vite": "^5.3.0"
  }
}
